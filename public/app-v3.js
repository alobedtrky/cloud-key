await import('/customer-auth.js');
const $=s=>document.querySelector(s),money=n=>`${Number(n).toFixed(2).replace('.00','')} ر.س`;const cats=[['all','الكل'],['games','الألعاب'],['subscriptions','الاشتراكات'],['software','البرامج'],['vpn','VPN'],['cards','البطاقات'],['services','الخدمات']];let products=[],active='all',coupon='',cart=JSON.parse(localStorage.getItem('ck-cart')||'[]');
function toast(t){const e=$('#toast');e.textContent=t;e.classList.add('show');setTimeout(()=>e.classList.remove('show'),2200)}function save(){localStorage.setItem('ck-cart',JSON.stringify(cart));renderCart()}function add(id){const p=products.find(x=>x.id===id);if(!p)return;const i=cart.find(x=>x.id===id);if(i){if(i.qty>=p.stock)return toast('وصلت للكمية المتاحة');i.qty++}else cart.push({id,qty:1});save();toast('تمت الإضافة للسلة')}
function renderProducts(){const q=$('#searchInput').value.trim().toLowerCase();const list=products.filter(p=>(active==='all'||p.category===active)&&(!q||p.name.toLowerCase().includes(q)||(p.description||'').toLowerCase().includes(q)));$('#productGrid').innerHTML=list.map(p=>`<article class="product-card" data-open="${p.id}"><div class="product-cover"><img class="product-image" src="${p.image}" alt="${p.name}"><span class="tag">${p.tag||'رقمي'}</span></div><div class="product-info"><h3>${p.name}</h3><div class="meta"><span>${p.description||'منتج رقمي'}</span><span>متوفر ${p.stock}</span></div><div class="price-row"><div class="price"><strong>${money(p.price)}</strong>${p.oldPrice?`<del>${money(p.oldPrice)}</del>`:''}</div><button class="add-btn" data-add="${p.id}">+</button></div></div></article>`).join('')||'<div class="empty-state">لا توجد منتجات مطابقة.</div>';document.querySelectorAll('[data-open]').forEach(c=>c.onclick=e=>{if(e.target.closest('[data-add]'))return;location.href='/product/'+encodeURIComponent(c.dataset.open)});document.querySelectorAll('[data-add]').forEach(b=>b.onclick=e=>{e.stopPropagation();add(b.dataset.add)})}
function renderFilters(){$('#filters').innerHTML=cats.map(c=>`<button class="filter-btn ${active===c[0]?'active':''}" data-filter="${c[0]}">${c[1]}</button>`).join('');document.querySelectorAll('[data-filter]').forEach(b=>b.onclick=()=>{active=b.dataset.filter;renderFilters();renderProducts()})}
function renderCart(){cart=cart.filter(i=>products.some(p=>p.id===i.id));$('#cartItems').innerHTML=cart.map(i=>{const p=products.find(x=>x.id===i.id);return`<div class="cart-item"><div><b>${p.name}</b><small>${money(p.price)} × ${i.qty}</small></div><div class="qty"><button data-dec="${i.id}">−</button><span>${i.qty}</span><button data-inc="${i.id}">+</button></div></div>`}).join('')||'<p class="section-note">السلة فارغة.</p>';document.querySelectorAll('[data-inc]').forEach(b=>b.onclick=()=>{const i=cart.find(x=>x.id===b.dataset.inc),p=products.find(x=>x.id===i.id);if(i.qty<p.stock)i.qty++;save()});document.querySelectorAll('[data-dec]').forEach(b=>b.onclick=()=>{const i=cart.find(x=>x.id===b.dataset.dec);i.qty--;if(i.qty<=0)cart=cart.filter(x=>x!==i);save()});const sub=cart.reduce((s,i)=>s+(products.find(p=>p.id===i.id)?.price||0)*i.qty,0);$('#subtotal').textContent=money(sub);$('#discount').textContent=coupon?`الكوبون: ${coupon}`:'0 ر.س';$('#total').textContent=money(sub);$('#cartCount').textContent=cart.reduce((s,i)=>s+i.qty,0)}
function openCart(){$('#cartDrawer').classList.add('open');$('#overlay').classList.add('open')}function closeCart(){$('#cartDrawer').classList.remove('open');$('#overlay').classList.remove('open')}
async function fillCustomer(){const u=await window.CloudKeyAuth.getUser();if(!u)return;const f=$('#checkoutForm');if(f.elements.name&&!f.elements.name.value)f.elements.name.value=u.displayName||'';if(f.elements.email){f.elements.email.value=u.email||'';if(u.email)f.elements.email.readOnly=true}if(f.elements.phone&&!f.elements.phone.value&&u.phoneNumber)f.elements.phone.value=u.phoneNumber}
async function requireLogin(){const u=await window.CloudKeyAuth.getUser();if(u)return u;location.href='/login?next='+encodeURIComponent(location.pathname+location.search);return null}
$('#openCart').onclick=openCart;$('#closeCart').onclick=closeCart;$('#overlay').onclick=closeCart;$('#searchBtn').onclick=()=>{$('#searchPanel').classList.toggle('open');$('#searchInput').focus()};$('#searchInput').oninput=renderProducts;$('#applyCoupon').onclick=()=>{coupon=$('#couponInput').value.trim().toUpperCase();toast(coupon?'سيتم التحقق من الكوبون عند الطلب':'أدخل كود الخصم');renderCart()};
$('#checkoutBtn').onclick=async()=>{if(!cart.length)return toast('أضف منتجًا للسلة أولًا');if(!await requireLogin())return;closeCart();await fillCustomer();$('#checkoutModal').showModal()};
$('#checkoutForm').addEventListener('submit',async e=>{e.preventDefault();const u=await requireLogin();if(!u)return;const fd=new FormData(e.target),btn=e.target.querySelector('[type=submit]');btn.disabled=true;btn.textContent='جاري إنشاء الطلب...';try{const token=await u.getIdToken();const r=await fetch('/api/checkout',{method:'POST',headers:{'content-type':'application/json',Authorization:`Bearer ${token}`},body:JSON.stringify({name:fd.get('name'),email:fd.get('email'),phone:fd.get('phone'),coupon,items:cart})});const out=await r.json();if(!r.ok)throw new Error(out.error==='phone_required'?'أدخل رقم جوال صحيح':out.error||'تعذر إنشاء الطلب');cart=[];save();coupon='';$('#checkoutModal').close();$('#orderSuccess').innerHTML=`<div class="ok"><b>تم إنشاء الطلب بنجاح</b><span>رقم الطلب</span><code>${out.orderNumber}</code><span>الإجمالي ${money(out.total)}</span><a class="primary-btn" href="/profile">عرض طلباتي</a></div>`;$('#orderSuccess').classList.add('show');toast(`تم إنشاء الطلب ${out.orderNumber}`)}catch(err){toast(err.message)}finally{btn.disabled=false;btn.textContent='إنشاء الطلب'}});
$('#trackBtn').onclick=async()=>{const number=$('#trackNumber').value.trim().toUpperCase();if(!number)return toast('أدخل رقم الطلب');const u=await requireLogin();if(!u)return;const box=$('#trackResult');box.textContent='جاري البحث...';try{const token=await u.getIdToken();const r=await fetch(`/api/order-status?number=${encodeURIComponent(number)}`,{headers:{Authorization:`Bearer ${token}`}});const o=await r.json();if(!r.ok)throw new Error('لم يتم العثور على الطلب في حسابك');const labels={pending_payment:'بانتظار الدفع',paid:'مدفوع',processing:'قيد التجهيز',completed:'مكتمل',cancelled:'ملغي'};box.innerHTML=`<div class="ok"><b>${o.number}</b><span>الحالة: ${labels[o.status]||o.status}</span><span>الإجمالي: ${money(o.total)}</span><span>${new Date(o.createdAt).toLocaleString('ar-SA')}</span></div>`}catch(e){box.textContent=e.message}};
async function boot(){const r=await fetch('/api/products');products=await r.json();renderFilters();renderProducts();renderCart();const u=await window.CloudKeyAuth.getUser();document.querySelectorAll('[data-account-link]').forEach(a=>{a.href=u?'/profile':'/login';a.textContent=u?'حسابي':'تسجيل الدخول'});if(new URLSearchParams(location.search).get('cart')==='1')openCart()}boot();